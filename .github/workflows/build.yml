# This workflow will do a clean install of node dependencies, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

name: ⚽ Build Image

on:
  push:
    branches:
      - main
    paths:
      - 'web/src/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Dockerfile
        uses: actions/checkout@v2
      # 使用Docker官方制作的action
      # https://github.com/marketplace/actions/build-and-push-docker-images
      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_NAME }}
          password: ${{ secrets.DOCKER_PASS }}
      - name: Build and push
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: yfsoftcom/coder-kit:latest
      - name: Image digest
        run: echo ${{ steps.docker_build.outputs.digest }}

      - name: update the container
        id: update_container
        uses: dawidd6/action-ansible-playbook@v2
        with:
          playbook: site.yml
          directory: ./devops
          key: ${{secrets.FPM_PRIVATE_SSH_KEY}}
          inventory: |
            all:
              children:
                hk:
                  hosts:
                    hk.yunplus.io
                  vars:
                    ansible_ssh_user: root
                    ansible_python_interpreter: /usr/bin/python3
        # uses: saubermacherag/ansible-playbook-docker-action@v1.4
        # with:
        #   playbookName: '/github/workspace/devops/site.yml'
        #   inventoryFile: '/github/workspace/devops/inventory-server.yml'
        #   keyFileVaultPass: ${{ secrets.FPM_PRIVATE_SSH_KEY }}